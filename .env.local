# Email Configuration Environment Variables
# Copy these to your .env file and update with your actual email credentials

# Email Provider (gmail, outlook, smtp)
EMAIL_PROVIDER=gmail

# Email Credentials
EMAIL_USER=send.sufiyan@gmail.com
EMAIL_PASS=jxqm scuj ahsq jsjo


# For SMTP Provider (only needed if EMAIL_PROVIDER=smtp)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

DATABASE_URL="postgresql://neondb_owner:Lkyz7Sbu0tCj@ep-summer-snowflake-a1epoigq-pooler.ap-southeast-1.aws.neon.tech/EvOrgsDevelopment?sslmode=require"

# Gmail Setup Instructions:
# 1. Enable 2-Factor Authentication on your Gmail account
# 2. Generate an App Password:
#    - Go to Google Account settings
#    - Security > 2-Step Verification > App passwords
#    - Select app: Mail, Select device: Other (Custom name)
#    - Use the generated 16-character password as EMAIL_PASS

# Outlook Setup Instructions:
# 1. Use your regular Outlook email and password
# 2. If you have 2FA enabled, you may need an app password

# Custom SMTP Setup Instructions:
# 1. Get SMTP settings from your email provider
# 2. Set EMAIL_PROVIDER=smtp
# 3. Configure SMTP_HOST, SMTP_PORT, and SMTP_SECURE accordingly
